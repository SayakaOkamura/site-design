# この案件の進め方

進め方そのもの（質問は指示ではない／迷ったら先に訊く／説明を足さない）は
**ユーザー全体の `~/.claude/CLAUDE.md`** に置いてある。ここには**この案件の話だけ**書く。

## 決めごとは NOTES.md に残す

案ごとの `designs/*/NOTES.md` が正。
**却下したものは理由ごと残す**（却下は永久ではない。003 の「レトロ風は却下」は 011 で撤廃された）。
本人の言葉は、**言い回しのまま**引く。

## 案は消さない

新しい案は `designs/013-*/` のように**番号を増やして足す。**前の案は残す。
持ち込むのは**失敗の記録だけ**で、決めごとは引き継がない（本人がそう決めた）。

## 中身は公式サイトの言葉

`alt-dsgn.co.jp` に出ている文が土台。**足さない**のが既定。
膨らませてよいと言われたときも、**膨らませるのは意見**であって、
**実績・数字・取引先・事例のような事実は作らない。**

## 確かめてから push

`localhost` で開いて、**画面を見てから**（`file://` は拡張が開けない）。
Pages は HTML を 10 分ほどキャッシュするので、渡す URL には `?v=` を足す。
窓は**一つでは足りない**——高さの低い窓で崩れることが何度もあった。
