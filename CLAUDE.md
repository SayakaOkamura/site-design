# 進め方

このファイルは**毎回かならず読まれる**。記憶ファイルは関連しそうなときしか引かれないので、
何度も同じことを言われている決めごとは、ここに置く。

## 質問は指示ではない

「**これ何？**」「**なんで**」「**意図は**」で来たものは**問い**。**答えるだけにする。**

直したほうがいいと思っても、**手を動かす前に「直しますか」と訊く。**
直してから「戻しますか」は遅い。もう既成事実ができている。

岡村さんは**直すときは直すと言う**（「〜して」「〜できる？」「〜にして」）。
その形で来ていないものは、直す合図ではない。

## 迷ったら、作る前に確認を取る

範囲・題材・言い回しで**判断が割れそうなとき**は、一度訊く。
**訊くのは安く、作り直しは高い。**

とくに割れやすいところ：
- **どこまで直すか**（一つだけか、同じ条件に当たるもの全部か）
- **例として出されたもの**を、そのまま使うのか、形だけ借りるのか
- **言葉の置き換え**（同じものを指す語を増やしていないか）

## 説明を足さない

画面に何か書く前に「**見れば分かることか**」を疑う。

「仮」「未定」「検討中」のような**申し送りは画面に置かない。**
それは NOTES とチャットに書くもので、サイトを見る人には意味がない。

## 決めごとは NOTES.md に残す

案ごとの `designs/*/NOTES.md` が正。**却下したものは理由ごと残す**（却下は永久ではない）。
本人の言葉は、言い回しのまま引く。

## 確かめてから push

`localhost` で開いて、**画面を見てから**。`file://` は拡張が開けない。
Pages は HTML を 10 分ほどキャッシュするので、渡す URL には `?v=` を足す。
